go.sh:可以直接启动三种运行模式，使用nohup，但是需要注意的是命令结尾不能带有&，试出来的。
启动三次运行.vbs:从windows端启动go.sh，使用nohup命令，而且输出的部分都会被投入到./data/log中，记得，这里要切换路径到包含main.py的目录作为主路径。还会自动启动监视程序。
监视程序运行情况.vbs:在运行源代码的时候，可以使用这个监视源代码的执行情况。
直接启动tensorboard.vbs:观察模型的运行效果，会自动打开浏览器。
启动虚拟环境.vbs:直接打开虚拟环境，下次就不用开xshell了。